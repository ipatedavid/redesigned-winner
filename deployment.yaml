apiVersion: v1
kind: PersistentVolume
metadata:
  name: nginx-pv
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /srv/redesigned-winner/games
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-pvc
  namespace: redesigned-winner
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  volumeName: nginx-pv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gamehub-deployment
  namespace: redesigned-winner
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gamehub-server
  template:
    metadata:
      labels:
        app: gamehub-server
    spec:
      containers:
      - name: nginx
        image: docker.io/ipatedavid/redesigned-winner:sha-ac3bdb5
        ports:
        - containerPort: 80
        volumeMounts:
        - name: games
          mountPath: /usr/share/nginx/html/games
      imagePullSecrets:
      - name: dockerhub-creds
      volumes:
      - name: games
        persistentVolumeClaim:
          claimName: nginx-pvc
---

apiVersion: v1
kind: Service
metadata:
  name: gamehub-service
  namespace: redesigned-winner
spec:
  type: NodePort
  selector:
    app: gamehub-server
  ports:
    - protocol: TCP
      port: 80          # Service port inside cluster
      targetPort: 80    # Container port
      nodePort: 30333   # External port on the node (must be 30000–32767)

